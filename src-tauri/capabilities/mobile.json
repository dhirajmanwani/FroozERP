{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile",
  "description": "FroozERP phone app permissions: core only. No updater or process plugin exists in a mobile build; the phone app is updated through its store.",
  "windows": ["main"],
  "platforms": ["android", "iOS"],
  "permissions": ["core:default"]
}
